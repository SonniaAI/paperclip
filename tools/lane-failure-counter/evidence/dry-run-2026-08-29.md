# SON-1536 WP-A dry-run receipt

Run date: 2026-08-29 UTC

Input: manager export `run-ledger-72h-20260829T1020Z.jsonl` from the SON-1536
evidence directory. The export is a 72-hour JSONL window with one metadata row
and 1,064 run rows across 18 lanes.

## Result

- Normalized records: **1,064**
- Invalid records: **0**
- Applied records: **1,064**
- Duplicate run IDs in the export: **0**
- Skipped records: **0**
- Outcome totals: `zero_write=421`, `terminal_fail=44`,
  `success_readonly=24`, `success=575`, `other=0`, `running=0`
- Persisted state: [`state.json`](./state.json), containing 1,064 processed run
  IDs and the required per-lane timestamps/streaks.

Active streaks after the chronological reduction:

| Lane | Current streak |
| --- | ---: |
| Product Development | 4 |
| Product Engineer | 3 |
| Reliability Senior Engineer | 1 |
| Sonnia | 7 |
| z-code | 11 |

All other lanes ended at streak 0 after a success or read-only success.

## Replay / read-only check

Re-running the same command against the persisted state in default dry-run
mode applied **0** records; all **1,064** run IDs were recognized as already
processed. A before/after SHA-256 check showed that the state file was not
modified by the dry run.

The manager's bridge tally is a separate trailing-24-hour window (86
zero-write deaths, including 35 WS-open-timeout deaths), while this receipt is
the requested 72-hour per-run reduction. The windows differ, but the export's
421/44/575/24 totals match its metadata and the reducer preserves the bridge's
zero-write-vs-terminal-fail distinction.
